package initialize

//import "fmt"

func SetupRPC() {

	 //port := viper.GetString("server.user.port")
	 //host := viper.GetString("server.user.host")
	 //address := host + port
	 //fmt.Println(address)
	 //// Set up a connection to the server.
	 //conn, err := grpc.Dial(address, grpc.WithInsecure())
	 //if err != nil {
	 //	log.Fatalf("did not connect: %v", err)
	 //}
	 //userClient := protobuf.NewUserServiceClient(conn)
	 //common.UserClient = userClient
}
